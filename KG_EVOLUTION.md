# Knowledge Graph (KG) 演化歷程

這份文件記錄了 WSM_RAG 專案中 Knowledge Graph (知識圖譜) 模組的開發歷程，詳細說明了從基礎原型到高效能、抗雜訊檢索元件的轉變過程。

## 第一階段：初始概念 (Inception) - "Bag of Entities" 方法
**目標：** 建立一個輕量級機制來捕捉向量檢索 (Vector Search) 可能遺漏的語意連結（特別是年份和專有名詞的精確匹配）。

*   **結構：** 實作為簡單的 **倒排索引 (Inverted Index)** (實體 Entity → 文件區塊索引集合 Set of Chunk Indices)。
*   **抽取邏輯 (v1)：**
    *   **年份 (Years)：** 使用正規表達式擷取 4 位數年份 (19xx, 20xx)。
    *   **詞彙 (Terms)：** *任何* 長度大於 2 的英文單字。
*   **權重設定：**
    *   年份：1.5 (高優先級)
    *   詞彙：1.0
*   **問題：** 這種「通吃」的抽取策略產生了巨大的雜訊。常見詞彙如 "revenue" (營收), "report" (報告), "the" (如果沒被過濾掉) 建立了過多且無意義的連結，導致圖譜的作用更像是關鍵字計數器，而非語意地圖。

## 第二階段：規則優化 (Refinement) - 訊號與雜訊的分離
**目標：** 透過過濾雜訊並聚焦於具鑑別力的實體，提升檢索的精確度。

*   **關鍵變更：**
    *   **嚴格實體抽取：** 修改正規表達式，只擷取 **首字母大寫的詞彙 (Capitalized Terms)** (例如 "Apple", "TSMC", "Paris")。這是基於在金融/正式文件中，重要實體通常是專有名詞的假設。
    *   **停用詞過濾 (Stopword Filtering)：** 加入了強大的停用詞清單，防止常見的句首詞 (如 "The", "In", "However") 汙染索引。
    *   **權重反轉 (Weighting Inversion)：**
        *   **專有名詞 (5.0)：** 大幅提升。特定公司名稱的匹配是相關性的強烈訊號。
        *   **年份 (1.0)：** 調降。雖然重要，但單獨一個年份 (如 "2023") 出現在太多文件中，不適合作為主要的排序因子。
*   **結果：** 圖譜變得更稀疏但準確度大幅提高。它開始發揮「命名實體錨點 (Named Entity Anchor)」的作用，能有效提升明確提及查詢中關鍵實體的文件排名。

## 第三階段：生產環境就緒 (Production Readiness) - 預先計算與驗證
**目標：** 消除執行時期的運算開銷，以滿足嚴格的延遲要求 (例如比賽提交環境)。

*   **效能瓶頸：** 隨著資料集增長，在執行時期從頭建構圖譜 (掃描所有區塊、執行 Regex/Jieba) 會消耗寶貴的秒數。
*   **解決方案：預先計算索引 (Pre-computed Index)**
    *   實作 `save()` 和 `load()` 方法，將倒排索引序列化為 JSON 檔 (`kg_index_en.json`, `kg_index_zh.json`)。
    *   **冷啟動 (Cold Start)：** 0 秒。系統能瞬間載入預先建立好的 JSON。
*   **安全機制 (自動驗證 Validation)：**
    *   **潛在問題：** 如果程式碼或資料集更新了，但 JSON 索引檔是舊的怎麼辦？
    *   **修復方案：** 加入 **索引不匹配檢測 (Index Mismatch Detection)**。
        *   載入器會檢查 JSON 中的 `Chunk Index` 是否超出了目前實際的文件區塊數量。
        *   如果偵測到不匹配 (例如：索引指向第 #1392 號區塊，但目前只有 1256 個區塊)，系統會記錄 `WARNING`，捨棄舊的索引，並 **自動退回執行時期建構 (Runtime Build)**。
    *   這確保了系統永遠不會因為索引不一致而崩潰，在必要時優先考量正確性而非速度。

## 架構總結
| 功能特性 | 第一階段 (Phase 1) | 第二階段 (Phase 2) | 第三階段 (Phase 3 - 目前) |
| :--- | :--- | :--- | :--- |
| **建構時間** | 執行時期 (慢) | 執行時期 (慢) | **預先計算 (瞬間)** / 自動退回 |
| **實體範圍** | 所有單字 | **首字大寫 / 專有名詞** | 首字大寫 / 專有名詞 |
| **雜訊程度** | 高 | 低 | 低 |
| **關鍵權重**| 年份 > 詞彙 | **詞彙 (5x) > 年份** | 詞彙 (5x) > 年份 |
| **可靠性** | N/A | N/A | **自動驗證與退回機制** |
| **語意擴展** | 無 | 無 | **詞幹還原 (Stemming) + IDF 動態權重** |

## 第四階段：優化 (Optimization) - 語意擴展與動態權重
**目標：** 進一步提升召回率 (Recall) 並解決固定權重的僵化問題。

*   **詞幹還原 (Stemming)：**
    *   引入 `NLTK PorterStemmer`。
    *   **解決問題：** 過去 "investing" 和 "investment" 被視為不同實體。現在統一還原為 "invest"，大幅增加檢索的命中率。
*   **動態權重 (TF-IDF)：**
    *   **引入 IDF (Inverse Document Frequency)：** $IDF = \log(1 + \frac{N}{df+1})$
    *   **機制：**
        *   如果一個實體 (如 "TSMC") 在所有文件中出現頻率很高，它的 IDF 值會降低，權重隨之下降。
        *   如果一個實體很稀有，權重會提升。
    *   **公式：** $FinalWeight = BaseWeight \times IDF$
    *   這讓系統能自動判斷哪些實體才是該查詢中「真正獨特」的關鍵字。

## 第五階段：上下文擴展 (Contextual Expansion) - Pseudo-Relevance Feedback (PRF)
**目標：** 模擬「聯想」能力，從初步檢索結果中挖掘潛在的關鍵字，以捕捉隱含的語意關聯。

*   **機制 (Pseudo-Relevance Feedback)：**
    1.  **初步檢索 (Initial Search)：** 使用原始查詢實體進行第一輪 KG 檢索。
    2.  **挖掘 (Mining)：** 分析排名最高的 N 個文件區塊 (Top-N Chunks)，找出其中最常共同出現的實體。
    3.  **擴展 (Expansion)：** 將這些「關聯實體」加入查詢中，但給予較低的權重 (例如 0.5x)。
    4.  **重排序 (Re-ranking)：** 結合原始實體與擴展實體進行最終評分。
*   **優勢：**
    *   不需要外部同義詞庫 (Thesaurus) 或大型模型。
    *   能動態適應該領域的特有詞彙關聯 (例如發現 "TSMC" 常與 "Wafer" 共現，即使查詢沒寫 Wafer 也能抓到)。
    *   **代價：** 需儲存 `Chunk -> Entities` 的正向索引 (Forward Index)，稍微增加記憶體與儲存空間。

## 第六階段：全域實體整合 (Global Entity Consolidation) - 同義詞解析 (Synonym Resolution)
**目標：** 解決多語言與縮寫導致的實體破碎問題 (e.g., "TSMC" vs "Taiwan Semiconductor" vs "台積電")，將不同稱呼統一為同一語意概念。

*   **機制：**
    1.  **離線聚類 (Offline Clustering)：** 在建置索引後，統計高頻實體。
    2.  **LLM 判別：** 使用 LLM 找出這些高頻實體之間的同義關係，生成 `synonym_map.json` (e.g., `{"台積電": "TSMC", "Taiwan Semiconductor": "TSMC"}`).
    3.  **查詢擴展 (Query Expansion)：**
        *   當使用者查詢 "台積電" 時，系統查表發現標準名是 "TSMC"。
        *   系統自動將 "TSMC" 加入查詢條件，確保能搜到只寫了英文名稱的文件。
*   **優勢：**
    *   **跨語言檢索：** 完美解決中英夾雜的財報檢索問題。
    *   **縮寫還原：** 自動連結全名與縮寫。

## 第七階段：回歸修正與效能優化 (Regression Fix & Scale-Up)
**目標：** 修正因 Phase 5/6 引入的雜訊導致分數下降問題，並擴展同義詞建構的規模。

*   **抗雜訊調優 (Noise Reduction)：**
    *   **降權 KG (De-weighting)：** 在 RRF 融合階段，將 KG 的權重從 1.0 降至 **0.3**。讓 KG 回歸「輔助」角色，僅在關鍵實體出現時提供加分，而非主導排序。
    *   **嚴格化 PRF (Stricter Expansion)：**
        *   **門檻提升：** 擴展實體必須在 **至少 2 個** Top-Chunks 中共同出現才被採納（原為 1 個）。
        *   **權重降低：** 擴展實體的權重從 0.5 降至 **0.2**，防止查詢漂移 (Query Drift)。
*   **同義詞建構優化 (Builder Optimization)：**
    *   **批次處理 (Batch Processing)：** 將 `scripts/build_synonyms.py` 升級為批次處理模式。
    *   **規模擴大：** 處理 Top-1000 高頻實體（原為 Top-200），大幅提升同義詞覆蓋率。
    *   **穩健性：** 加入錯誤處理與 JSON 格式驗證。
